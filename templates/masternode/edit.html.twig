{% extends 'base.html.twig' %}

{% block title %}Edit Masternode{% endblock %}

{% block body %}
    <h1>Edit Masternode</h1>

    {{ include('masternode/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('masternode_index') }}">back to list</a>

    {{ include('masternode/_delete_form.html.twig') }}
{% endblock %}
